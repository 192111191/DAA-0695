#include<stdio.h>
int main()
{
	int num,count=0,i;
	printf("enter the number:");
	scanf("%d",&num);
	for(i=1;i<=num; i++)
	{
		if(num%i==0)
		count+=1;
	}
	if(count>2)
		printf("not prime");
	else
		printf("  prime");
}
